import 'package:flutter/material.dart';

class IconConstants {
  static const Icon personIcon = Icon(Icons.person);
  static const Icon phoneIcon = Icon(Icons.phone);
  static const Icon contactIcon = Icon(Icons.perm_contact_cal);
  static const Icon addIcon = Icon(Icons.add_outlined);
  static const Icon groupIcon = Icon(Icons.group);
  static const Icon groupAddIcon = Icon(Icons.group_add);
  static const Icon ruppeeIcon = Icon(Icons.currency_rupee_sharp);
  static const Icon descriptionIcon = Icon(Icons.description);
  static const Icon signupIcon = Icon(Icons.person_add);
}
